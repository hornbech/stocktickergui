services:
  stockoverview:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./config:/data
    environment:
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-}
    restart: unless-stopped
